<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentHome">

    <fragment
        android:id="@+id/fragmentHome"
        android:name="android.example.contactlesscheckout.FragmentHome"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_fragmentHome_to_cameraActivity"
            app:destination="@id/cameraActivity" />
    </fragment>
    <activity
        android:id="@+id/cameraActivity"
        android:name="android.example.contactlesscheckout.CameraActivity"
        android:label="activity_camera"
        tools:layout="@layout/activity_camera" >
    </activity>
</navigation>